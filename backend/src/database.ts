// Legacy file — database is now managed entirely by Prisma (PostgreSQL)
// This stub is kept to avoid breaking any imports that may reference this module.
// All actual database operations go through the Prisma client in src/repositories/

export default {};
