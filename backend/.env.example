# Environment Variables for Avana RMA Backend

# Server Configuration
PORT=3001
NODE_ENV=production

# JWT Configuration (CRITICAL - CHANGE IN PRODUCTION)
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-long-random-secret-key-change-this-in-production-min-32-characters

# CORS Configuration
# Set to your deployed frontend URL (comma-separated for multiple)
CORS_ORIGIN=https://your-app.vercel.app

# Database (PostgreSQL for production - get from Neon.tech)
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Frontend URL (for password reset links)
FRONTEND_URL=https://your-app.vercel.app

# Email Configuration (Future use)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
